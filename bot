import discord

client = discord.Client()

@client.event
async def on_message(message):
    message.content.lower()
    if message.author == client.user:
        return
    if message.content.startswith("twitch1"):
        await message.channel.send("BenPhillipsUK is LIVE on twitch click here: https://www.twitch.tv/benphillipsuk")
    if message.author == client.user:
        return
    if message.content.startswith("twitch2"):
        await message.channel.send("ElliotGilesGaming is LIVE on twitch click here: https://www.twitch.tv/elliotgilesgaming")
    if message.author == client.user:
        return
    if message.content.startswith("twitch3"):
        await message.channel.send("Connorthomas3d is LIVE on twitch click here: https://www.twitch.tv/connorthomas3d")
    if message.author == client.user:
        return
    if message.content.startswith("-merch"):
        await message.channel.send("Remember and head over to https://sorrybro.com and purchase your copy of Ben's book.")
    if message.author == client.user:
        return
    if message.content.startswith("=youtube"):
        await message.channel.send("New video is up go head over and check it out: https://www.youtube.com/user/benphillipsuk/videos #SORRYBRO")
    if message.author == client.user:
        return
    if message.content.startswith("typesend"):
        await message.channel.send("Hi Ladies and Gentlemen I am the official SORRY BRO BOT. I will feature announcements in most of the channels with the help of those awesome brainy Tech Support guys. Stay tuned for updates as they come through #SORRYBRO")
    if message.author == client.user:
        return
    if message.content.startswith("00prank"):
        await message.channel.send("All your prank ideas are awesome folks. Keep them coming!! #SORRYBRO")
    if message.author == client.user:
        return
    if message.content.startswith("£SB"):
        await message.channel.send("Welcome to the OFFICIAL Ben Phillips UK Server. I am the Official SORRY BRO BOT. The team would like you all to know that you are all absolute LEGENDS. Stay safe and see you all on twitch #SORRYBRO")
    if message.author == client.user:
        return
    if message.content.startswith("=sneak"):
        await message.channel.send("Hello everyone. As you have all noticed I am now active on the server. You all now know me as The Official SORRY BRO BOT. My precence in the server may have came as a surpise to you all but thanks to the Tech Support crew others are not far behind. Stay tuned!!! #SORRYBRO")
    if message.author == client.user:
        return








client.run("NzE0MzM0ODI0MTA1NjM5OTQ3.XtAN8A.cBRN7T33TTKqo6osSHKprf-lDdQ")
